# MusicalInstrumentClassifier
CNN model for classifying musical instruments. Trained with NSynth dataset.

Drop the file in the same directory as the folders containing the dataset and run from there.
